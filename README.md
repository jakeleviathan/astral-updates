# ASTRAL Updates Channel

This public repository hosts update feeds and release assets for ASTRAL desktop clients.

- `appcast-macos.xml` is consumed by macOS clients.
- `appcast-windows.xml` is consumed by Windows clients.
- `appcast.xml` is a legacy alias for older macOS builds.

Source code remains in a separate repository and can be private.
